import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Button = () => {
  return (
    <View>
      <Text>Button</Text>
    </View>
  );
};

export {Button};

const styles = StyleSheet.create({});
